<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:id="@+id/rlAdd_Teilaufgabe"
                android:layout_height="wrap_content">

<ImageButton
        android:id="@+id/ibAddTeilaufgbe"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:tint="@color/colorDivider"
        android:background="@color/transparent"
        android:src="@drawable/ic_add_toolbar"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        style="@style/Teilaufgaben_TextView"
        android:layout_toRightOf="@id/ibAddTeilaufgbe"
        android:text="@string/AddTeilaufgabe"
        android:textColor="@color/colorDivider"/>

    <!--
    <include
        layout="@layout/divider_teilaufgaben"
        />
        -->

</RelativeLayout>